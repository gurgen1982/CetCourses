@model IEnumerable<CetCources.Database.YearGroup>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>List of Age Groups</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GroupName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GroupName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.YearGroupId }) |
            @Html.ActionLink("Details", "Details", new { id=item.YearGroupId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.YearGroupId })
        </td>
    </tr>
}

</table>
